[pytest]
DJANGO_SETTINGS_MODULE = core.settings.testing
# The Django project lives under backend/; add it to sys.path so `core` imports
# resolve when pytest is invoked from the repository root (CI, scripts/ci.sh).
pythonpath = backend
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = backend
